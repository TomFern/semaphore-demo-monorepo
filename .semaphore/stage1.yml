version: v1.0
name: Pipeline 4
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Stage ${{ parameters.SVC }} to Heroku'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "job 1"
  - name: 'Block #2'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo yes
